---
title: 面试问题精选总结
tags: ['面试']
---

### 自我介绍及项目介绍
#### 自我介绍
自我介绍通常包含以下几个方面，基本信息，工作履历（强调表达能力、沟通能力），工作技术和领域，对岗位的期待，以下为样例：
```
面试官好，我是东南大学 2021 届毕业生，毕业后加入浪潮通信信息工作3年多，于去年10月份离职。
在浪潮工作期间，我前后参与了两个项目组的工作。这主要是由于我的学习能力和沟通表现都非常出色，所以在我工作一年后就调到第二个项目组担任核心开发，也就是DCIM项目组，
在这里我参与了系统从0到1的建设，并参与了核心模块的重构与开发，这也使得我的Java基础更加坚实，并进一步接触到了springboot、springcloud这样的框架，也对分布式，微服务有了一定的实战经验，包括一些常用中间件redis、kafka都有过研究。
最后我期待这次找工作能够加入一个研发体系更加成熟的团队，遇到更具有挑战性的项目和同事一起攻克技术难题，创造更大的价值吧

```

#### 项目介绍
项目介绍需要遵循SRTP法则，以下为样例
DCIM项目：
DCIM平台当时建设的目标是帮助移动运营商实现数据中心高效运营，我们做了有基础设施监控、能耗管理、资产管理以及运维管理等功能模块。我们主要负责建设江苏省级综合平台，计划里涉及到3个大型数据中心、十几个小型数据中心，还有几个市级数据中心的独立系统搭建。我们系统后端是使用了分布式微服务的架构，使用了Spring Cloud框架，我在项目中负责了好几块内容：
第一是设备监控数据的对接，原本我们有套支持独立系统的采集程序，基于Netty和SpringMVC开发，然后我拿到之后是考虑第一是技术栈确实比较老旧不便于维护，二是原本采集程序里面涉及了多份不同的协议整体也比较臃肿，所以我选择使用SpringBoot开发重构整个采集程序，最后实现了支持百万级传感器设备的采集，并利用Kafka异步处理设备数据，设立了不同的消费者组去处理数据，；
其次我也负责了部分页面的可视化接口开发，这里我用了redis缓存数据来提高查询速度。
还有就是在系统运行起来之后，我们也要继续进行功能升级迭代和维护工作，比如提高监控覆盖率接入更多的数据中心，根据用户需求优化功能等等，期间呢甲方对我们的工作也比较认可，也写过感谢信

网优平台：
这个网优平台确实有点特殊，它是运营商网络运维的核心系统，已经开发了十几年，功能模块特别多，覆盖全省超千万级基站的实时数据，像无线信号、设备性能这些维度的数据都在里面。基于这些数据，系统能实现数据分析、故障预警和工单闭环这些关键功能，支撑着日常的网络运维工作。​
我刚加入团队的时候还是个新人，毕竟平台业务深，我接触的时间不算长，负责的功能相对独立。主要有两块：一块是可视化大屏的后端接口开发，当时做了好几个运维监控大屏，得把基站的各类数据实时传到前端展示；另一块是数据导入导出的后端实现，因为很多数据分析结果，用户都要导出成 Excel 表格做报告，这部分需求还挺频繁的。​
那时候做导出功能时，一开始用普通方法处理，数据量大的时候特别慢，用户反馈好几次。后来我就想用 Easyexcel 来做，再结合自定义的线程池，加上 countdownlatch 控制并发，这么一优化，导出速度比之前快了不少，用户后面没再提过导出卡壳的问题。虽然负责的功能不算特别核心，但这些独立模块的开发，让我快速熟悉了系统的数据流转逻辑，也练了练并发处理的实际用法。​
其实这个平台太大了，我确实没法把所有模块都说清楚，但我负责的这两块功能，都实实在在解决了用户的日常需求，也算给团队帮上了忙。

#### 难点与亮点
1. 优化监控数据阈值预警处理逻辑，由原先基于 SQL 的批量查询处理方式迁移至后端逻辑计算，整体预警处理耗时从原先 5 分钟缩短至 30 秒内：
这是一个独立的数据中心系统提出的功能，一开始告警功能由MySQL存储过程，但原本在SQL实现是需要定时对指标实时数据表进行全表扫描，用join去筛选规则范围内的指标，再进行判断。一方面是指标表数据量很大，整个告警判断的时间较长无法满足用户的需求，其次是在SQL存储里面写业务后期并不好维护。所以我们就计划把这块的业务迁移到后端java里面去做。那么其实在Java里面单机扫表的性能可能还是不能满足需求，会导致任务堆积告警不及时，所以为了解决这个问题采用多线程扫表的方案，基于了xxl-job的分片功能，充分利用集群中的实例进行任务处理。xxl-job是支持分片广播的

1. 基于 Netty 实现与动环监控系统的自定义通信协议接入，采集百万级传感器监控数据，使用 Kafka 消息队列异步分
发，分别进行 MySQL 设备数据更新与 TDengine 时序数据库入库。
DCIM平台建设主要依托于监控数据，所以整套监控数据采集存储系统就非常重要。我也参与整个核心模块的设计，包括采集层的优化，kafka消息处理入库开发。目标呢是支持多个数据中心百万级别监控点采集，延迟控制在1s内。
首先在采集程序这里
是作为客户端向多个数据中心的动环系统主动发起连接并采集数据，核心挑战是百万级监控点带来的高并发连接和数据解析压力，所以我们选择基于Netty去开发，按照设备或监控点采用分片将百万级设备均匀分配到不同的Netty节点去采集，在实现过程中，我们对Netty使用的线程池做了优化，严格区分IO线程和业务线程，让IO线程只负责网络读写，
数据解析、格式转换、还有推送kafka这种耗时操作都交给单独的业务线程池处理，避免IO线程被阻塞。
然后在数据传输环节，使用了kafka哈希分区策略，保证每个分区的负载相对均匀，并且分区数匹配对应的消费者数量，随后由消费者处理数据入库，在数据存储方面使用了分层策略：MySQL使用多线程批量入库更新操作仅保留最新监控值，使用了主从架构将读写分离，因为也有可视化页面的呈现需要读这份数据，这样保证了更高并发；部分监控点的历史数据则存储在Tdengine中。最后项目实际上线，在支持了3个大型数据中心，100多万的监控点采集正常，同时保证了监控可视化和历史查询功能的需求都在用户要求范围内。

   * Mysql库： 主从复制
   基于binlog实现，从服务器开启主从复制后，创建出io线程和sql线程，io线程用于和主库建立连接，
   I/O线程拉取bin-log并保存在relay-log
   SQL线程解析 relay-log

   * 线程池的原理
  

   * kafka消息可靠性
   如何保证可靠性，不丢失，kafka是个消息中间件，需要结合生产者和消费者进行工作，所以需要保证消息不丢失，也需要从三个方面一起处理。 比如生产者可以在异步发送时传入一个回调函数支持重试，也可以设置一些参数提高发生成功率，比如重试次数，ack参数，
   kafka集群也有一些机制保证消息不丢失，比如使用持久化存储，写入kafka的数据将持久化到磁盘，ISR复制机制，为分区设立多个副本，而最后的消费者端则需要确保传递过来的消息需要正常消费不要胡乱提交偏移量。






1. 
#### 详细说下在项目中的工作吧
1. 工作：基于 Netty 实现与动环监控系统的自定义通信协议接入，采集百万级传感器监控数据，使用 Kafka 消息队列异步分
发，分别进行 MySQL 设备数据更新与 TDengine 时序数据库入库，单批数据采集吞吐量提升至 50W+ 条/分钟；
描述：与上文难点和亮点相同

1. 工作：优化监控数据阈值预警处理业务，由原先基于 MySQL 存储过程的方式迁移至后端处理，整体预警处理耗时从原先 5分钟 缩短至 30秒内；

描述：与上文难点和亮点相同

3. 工作：负责数据中心数据可视化后端接口开发，使用 Redis 缓存部分监控数据，接口响应时间缩短至 50ms 以内。
描述：使用什么样的数据结构；更新是什么策略，

1. 工作：优化平台登录系统，基于 Spring Security 集成中国移动集团 SIM 卡认证机制，实现无感知自动登录
描述：

1. 工作：负责可视化大屏后端接口开发，使用 EasyExcel 实现百万级数据的批量导出，结合自定义线程池与 CountDownLatch 进行并发优化，整体数据导出性能提升3倍以上，500 万条数据导出时间缩短至1 分钟内
描述：
这个问题的背景是产品在初期设计时在一个独立模块中选择通过excel上传和下载基站统计数据，涉及到导出的数据量有将近上百万。那么当处理大数据量的excel文件时常用的poi库可能因为内存占用较高导致内存溢出问题，同时数据处理过程可能非常耗时，导致用户等待时间过长或请求超时，所以为了解决这个问题我主要做了两方面的工作，第一如简历中所说尽量提高导出速度，首先对数据分片，设定每个线程处理的数据量，然后创建自定义线程池，每个线程池负责查询一部分数据，并将结果写入内存中的额临时文件，主线程用countdownlatch协调等待所有分片任务处理完，再执行主线程将所有分片数据合并生成最终的文件。 然后呢数据库层面上也可以对表加索引提高查询速度。第二呢就是将整个点击下载过程改为异步操作，用户不需要在页面一直等待，而是文件生成后直接发到用户邮箱，那么也是一个优化用户体验的方法。


#### 还有什么想问的
我想知道这个团队具体负责哪些领域，如果我能去的话会让我负责哪些模块呢？会有人指导帮助我快速落地嘛？还是有非常详实的文档，那您觉得我需要提前学习或者准备点什么，以方便我能快速上手嘛



#### Java八股 
以下为和豆包ai进行模拟面试记录的部分问题：

##### Java基础：具备扎实的 Java 基础，熟悉常见容器（如 ArrayList、HashMap、LinkedList）的底层原理

##### Java并发：掌握线程池、锁（如 synchronized、CAS、AQS 等）
* 你提到熟悉 Java 并发编程中的锁，比如 synchronized 和 CAS，能说说 synchronized 和 CAS 在实现原理上的核心区别吗？它们分别适用于什么场景？

synchronized是JVM内置锁，通过monitor对象实现，JDK 1.6后引入了锁升级机制（偏向锁→轻量级锁→重量级锁），轻量级锁阶段也会自旋尝试获取锁，仅在自旋失败后才升级为重量级锁（挂起线程）；而CAS是通过Unsafe类的native方法实现的乐观锁，依赖CPU的原子指令，通过循环自旋尝试修改值，不会挂起线程。适用场景上，CAS更适合锁持有时间短、竞争不激烈的场景，此时自旋开销小于线程切换；但CAS存在ABA问题（可通过版本号解决）和自旋过度消耗CPU的问题。synchronized则适合锁持有时间长或竞争激烈的场景，因为此时JVM会升级为重量级锁，通过操作系统调度挂起线程，避免自旋浪费CPU，且无需手动处理ABA等问题，使用更简单稳定


* 关于线程池的corePoolSize、maximumPoolSize、workQueue，能详细说说它们的协作关系吗？比如当任务提交速度远大于线程处理速度时，线程池会如何处理?

1. 若当前线程数＜`corePoolSize`，无论是否有空闲线程，都会创建核心线程执行任务；  
2. 若当前线程数≥`corePoolSize`，则将任务放入`workQueue`：  
   - 若队列未满，任务等待核心线程处理；  
   - 若队列已满，且当前线程数＜`maximumPoolSize`，则创建非核心线程执行任务；  
   - 若队列已满且线程数≥`maximumPoolSize`，则触发拒绝策略。  
当任务提交速度远大于处理速度时：  
- 若使用**有界队列**（如ArrayBlockingQueue），队列满后会创建非核心线程，直到达到`maximumPoolSize`，之后触发拒绝策略；  
- 若使用**无界队列**（如LinkedBlockingQueue），队列会持续堆积任务，此时`maximumPoolSize`失效（不会创建非核心线程），最终可能因任务过多导致OOM。  
此外，非核心线程在空闲时间超过`keepAliveTime`后会被销毁，避免高负载后资源闲置，这也是三者协作中资源动态调整的重要部分。


* 你提到熟悉 AQS，能说说 AQS 的核心结构（如同步队列、状态变量等）以及它是如何实现独占锁（如 ReentrantLock）的吗?
  


* 你提到熟悉 Java 并发编程，能说说什么是线程安全吗？在项目中，你是如何保证多线程操作共享资源时的线程安全的？可以结合你做过的项目（比如 DCIM 平台中处理百万级传感器数据的场景）举例说明

线程安全指多线程并发访问共享资源时，无论线程调度顺序如何，程序的执行结果都与单线程执行结果一致，本质是保证操作的原子性、可见性和有序性。

* 关于 Java 中的volatile关键字，你了解它的作用吗？它能保证原子性吗？为什么？
volatile用于修饰变量使得该变量可以在多线程中共享，volatile并不是锁不能保证原子性。


##### JVM基础：了解 JVM 内存结构、垃圾回收机制和类加载机制

##### Spring框架
* spring aop实际使用场景
  说个我在项目中自己做过功能，就是使用AOP和注解结合实现参数校验，首先创建自定义注解